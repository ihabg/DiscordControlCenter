<UserControl x:Class="DiscordControlCenter.App.Views.AutomationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:automation="clr-namespace:System.Windows.Automation;assembly=PresentationCore"
             Background="{StaticResource ContentBackgroundBrush}">
    <Grid Margin="24,22">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,14"><TextBlock Text="Join automation" Style="{StaticResource PageHeadingStyle}" /><TextBlock Text="Constrained member-join workflows only. No scripts, bulk role actions, or automatic DM campaigns." Style="{StaticResource SecondaryTextStyle}" Margin="0,4,0,0" /></StackPanel>
        <Grid Grid.Row="1"><Grid.ColumnDefinitions><ColumnDefinition Width="360" MinWidth="280" /><ColumnDefinition Width="12" /><ColumnDefinition Width="*" MinWidth="360" /></Grid.ColumnDefinitions>
            <Border Style="{StaticResource PanelSurfaceStyle}"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions><StackPanel Orientation="Horizontal"><Button Content="New draft" Command="{Binding CreateDraftCommand}" Style="{StaticResource PrimaryButtonStyle}" automation:AutomationProperties.Name="Create join automation draft" /><Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0" /></StackPanel><ListBox Grid.Row="1" ItemsSource="{Binding Rules}" SelectedItem="{Binding SelectedRule}" Background="Transparent" BorderThickness="0" Margin="0,14,0,0" automation:AutomationProperties.Name="Join automation rules"><ListBox.ItemContainerStyle><StaticResource ResourceKey="DataRowContainerStyle" /></ListBox.ItemContainerStyle><ListBox.ItemTemplate><DataTemplate><StackPanel Margin="8"><TextBlock Text="{Binding Name}" FontWeight="SemiBold" /><TextBlock Text="{Binding State, Converter={StaticResource FriendlyDisplayConverter}}" Style="{StaticResource MutedTextStyle}" /></StackPanel></DataTemplate></ListBox.ItemTemplate></ListBox></Grid></Border>
            <GridSplitter Grid.Column="1" Width="12" Background="Transparent" />
            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto"><StackPanel><Border Style="{StaticResource PanelSurfaceStyle}" Padding="18"><StackPanel><TextBlock Text="WORKFLOW PRE-FLIGHT" Style="{StaticResource DetailLabelStyle}" /><TextBlock Text="{Binding ContextMessage}" Style="{StaticResource SecondaryTextStyle}" TextWrapping="Wrap" Margin="0,10,0,0" /><TextBlock Text="{Binding SelectedRule.Name}" Style="{StaticResource SectionHeadingStyle}" Margin="0,14,0,0" /><TextBlock Text="{Binding SelectedRule.SafeDescription}" Style="{StaticResource MutedTextStyle}" TextWrapping="Wrap" Margin="0,4,0,0" /><StackPanel Orientation="Horizontal" Margin="0,14,0,0"><Button Content="Save draft" Command="{Binding SaveDraftCommand}" Style="{StaticResource PrimaryButtonStyle}" automation:AutomationProperties.Name="Save join automation draft" /><Button Content="Validate draft" Command="{Binding ValidateCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0" automation:AutomationProperties.Name="Validate join automation draft" /></StackPanel><ItemsControl ItemsSource="{Binding PreflightIssues}" Margin="0,12,0,0"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding}" Style="{StaticResource SecondaryTextStyle}" TextWrapping="Wrap" Margin="0,3" /></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><TextBlock Text="{Binding StatusMessage}" Style="{StaticResource MutedTextStyle}" TextWrapping="Wrap" Margin="0,10,0,0" /></StackPanel></Border><Border Style="{StaticResource PanelSurfaceStyle}" Padding="18" Margin="0,12,0,0"><StackPanel><TextBlock Text="SAFETY BOUNDARIES" Style="{StaticResource DetailLabelStyle}" /><TextBlock Text="Enabling will require a future exact summary and typed confirmation. Each join must re-run connection, intent, member completeness, permissions, hierarchy, and duplicate-execution checks." Style="{StaticResource SecondaryTextStyle}" TextWrapping="Wrap" Margin="0,8,0,0" /></StackPanel></Border></StackPanel></ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
